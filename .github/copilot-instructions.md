Read AGENTS.md at the repository root — it is the master context file for
this repo: hard rules (zero dependencies, sync-by-design, fail-open vs
fail-closed contracts), module map, testing and release process.
